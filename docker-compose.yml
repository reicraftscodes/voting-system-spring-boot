services:
  mysql:
    image: mysql:latest
    container_name: pinkCatContainer
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: project01
      MYSQL_DATABASE: pinkCat
    volumes:
      - /Users/lauratemay/Documents/Learnings/Spring Boot & AI.I/voting/db:/var/lib/mysql
